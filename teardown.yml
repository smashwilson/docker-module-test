---

- name: remove all containers
  docker: state=absent image=debian:jessie
  tags: ["teardown"]

- name: remove any testimage containers
  docker: state=absent image=testimage:latest
  tags: ["teardown"]
